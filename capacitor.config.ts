import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alexcataguases.login',
  appName: 'loginAngularBlank',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
